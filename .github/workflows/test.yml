name: Update Chromium Layer
on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Deployment to perform: (develop, stage, preprod, prod)'
        required: true
        default: 'develop'

jobs:
  build:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy Test
        run: .github/test/test_deployer.sh
